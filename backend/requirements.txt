fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
yfinance>=0.2.54   # keep current — Yahoo breaks old versions regularly (pip install -U yfinance)
curl_cffi>=0.7.4
httpx==0.27.2
anthropic==0.39.0
langgraph==1.2.7
# Phase 3 — persistence & auth
sqlalchemy>=2.0
bcrypt>=4.0
PyJWT>=2.8
# Phase 4 — scheduled daily summaries
apscheduler>=3.10,<4.0
# Phase 2 (security) — per-IP rate limiting on expensive/sensitive endpoints
limits>=3.0
# Phase 4 (calibration) — Brier score, reliability curve, Platt-scaling fit
numpy>=1.26
# Learn-tab narration — free neural TTS (Microsoft Edge voices, no API key).
# Falls back to macOS `say` if unavailable/offline.
edge-tts>=6.1.9
# Tests — test_phase3/test_phase4 use pytest; the rest also run as
# `python -m tests.<name>`. See README "Running the tests".
pytest>=8.0
